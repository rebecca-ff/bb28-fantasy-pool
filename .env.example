# Project URL is already set in src/storage.js — you only need the anon key.
# Add this in Vercel → Settings → Environment Variables (and .env for local dev):
VITE_SUPABASE_ANON_KEY=your-anon-public-key
# Optional override:
# VITE_SUPABASE_URL=https://ejocfsuopcdaulgmskjs.supabase.co
